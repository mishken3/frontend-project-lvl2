#### step 8

info:

## Вывод в json

Кроме неструктурированного вывода (как текст), часто бывает нужен вывод в структурированном формате, таком как json. Это позволяет другим программам использовать ваш вывод для своей работы.

gendiff --format json filepath1.json filepath2.json

<!-- здесь вывод в формате json -->
<!-- он тут не приводится чтобы не раскрывать внутренние детали реализации -->

Задача:

- Напишите тесты.
- Реализуйте форматер json.
- Добавьте в ридми аскинему с примером работы пакета.

Подсказки:

- Как пример, посмотрите вывод команды eslint --format json .
